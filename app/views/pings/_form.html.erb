<%= form_with(model: ping) do |form| %>
  <% if ping.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ping.errors.count, "error") %> prohibited this ping from being saved:</h2>

      <ul>
        <% ping.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :poker_id, style: "display: block" %>
    <%= form.text_field :poker_id %>
  </div>

  <div>
    <%= form.label :task_id, style: "display: block" %>
    <%= form.text_field :task_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
